# apps/web

Next.js + TypeScript UI: review inbox, approvals, template preview, reports (see
`docs/ARCHITECTURE.md` §13 open decision #1). Lands once the REST API exists
(phase 1) and there is something to review (phase 2+) — see `docs/ROADMAP.md`.
